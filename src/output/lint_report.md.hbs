## Eugene 🔒 lint report of `{{#if name}}{{name}}{{else}}unnamed{{/if}}`

{{#unless skip_summary}}
This is a human readable SQL script safety report generated by [eugene](https://github.com/kaaveland/eugene).
Keep in mind that lints can be ignored by adding a `-- eugene: ignore E123` comment to the SQL statement
or by passing --ignore E123 on the command line.

{{#if passed_all_checks}}
The migration script passed all the checks ✅
{{else}}
The migration script did not pass all the checks ❌
{{/if}}

{{/unless}}
{{#each statements}}
### Statement number {{this.statement_number}}
#### SQL
```sql
{{this.sql}}
```
  {{#if this.triggered_rules}}
#### Lints
    {{#each this.triggered_rules}}

##### [{{this.name}}]({{this.url}})

ID: `{{this.id}}`

{{this.condition}}. {{this.effect}}. A safer way is: {{this.workaround}}.

{{this.help}}.
    {{/each}}
  {{else}}
No checks matched for this statement. ✅
  {{/if}}
{{/each}}
